import math
class Escape:
    # (x, y) : 현위치
    # 왼 아래 꼭짓점 (0, 0), 오른 위 꼭짓점 (w, h)
    def solve(self, x, y, w, h):




if __name__ == "__main__":
    a = Escape()
    x, y, w, h = map(int, input().split())
    a.solve(x, y, w, h)